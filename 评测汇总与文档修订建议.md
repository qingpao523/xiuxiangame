# 封神·放置修仙 — 评测汇总与文档修订建议

> 汇编：游戏开发工作室主理人 · 游承峰
> 日期：2026-08-18
> 范围：GAME-XIUXIAN（Web 版为主实现，Godot 已归档）
> 文档性质：**仅评测、零文件改动**。本文件为「多 Agent 六维评估」与「Playwright 加速全弧泡测」两轮的合并汇编，并给出**评测后建议的文档修改项清单**。
> ⚠️ 本轮未对任何代码或现有文档执行写入/编辑，所有「修改项」均为**建议**，待你确认后由对应工作流落地。

---

## 0. 阅读指引

| 你关心什么 | 看哪节 |
|---|---|
| 这几轮到底评了什么、总判定如何 | §1 评测总览 |
| 六维结论 + 相比原评估的修正 | §2 评测结论明细 |
| 哪些文档该改、改成啥、优先级 | §3 文档修改项清单（核心交付） |
| 我到底有没有动文件 | §4 不修改说明 |
| 接下来建议怎么走 | §5 建议的下一步 |

---

## 1. 评测总览（这几轮做了什么）

### 1.1 第一轮 · 多 Agent 六维评估（产物：`评估与门控建议.md`）
独立只读评审，覆盖 **设计 / 工程 / QA / 美术 / 发布 / 音频** 六维，强度为 full 完整门控。

- **设计**：反馈层 PASS／核心循环 CONCERNS／身份层 CONCERNS／叙事非线性 FAIL
- **工程**：双库漂移 FAIL／架构·审计·扩展·性能 CONCERNS
- **QA**：纪律 PASS／体感 CONCERNS／可玩放行 FAIL（"做完没玩过"）
- **美术**：基调 PASS＋4 条 CONCERNS
- **发布**：上架路径 CONCERNS／版本·云存档·本地化·灰度·运营门控全 FAIL
- **音频**：FAIL（零音频）

主理人随后对 4 处代码级结论做行级实证修正（开局序列、榜文、封神图卷、音频缺失），形成 §4 风险清单与 §5 五门路线。

### 1.2 第二轮 · Playwright 加速全弧泡测（产物：`playtest-report.md` + `playtest-report.json`）
以 Chromium headless 驱动 Web 版（`?debug=1` + `Game.fastForward()` 加速），目标为**全弧遍历**：开局 → 炼气士（rq_01–rq_10）→ 真人（zr_01–zr_10）→ 地仙（dx_01）。

**结果：工程侧 QA 判定 PASS。**
- 21 个境界全部可达，到达 `dx_01`；
- 2 次破劫 2 胜，战斗数值可赢、无崩溃；
- 种族 / 本命法宝 / 道友(6) / 机缘(4) / 目标 / 破劫 均被触发；
- 全程 **0** console error、**0** pageerror、**0** 失败请求、**0** 软锁。

> 注：初版机器人曾吃下 2995 连败、卡在 rq_10。经排查找出是**两处测试台缺陷**而非游戏缺陷（详见 §2.3）。修复后全弧直达地仙，证明战斗数值本身健全。

### 1.3 合并总判定
- **"能不能跑通 / 有没有报错 / 数值能不能赢"** → 已被 Playwright 实证为 **可放行**（原 QA 维度"可玩放行 FAIL"应上调，见 §2.1）。
- **"好不好玩 / 手感 / 节奏 / 沉浸"** → Playwright 机械驱动**不评估**，仍须真人试玩（你作为主理人的核心验收）。
- **结构性空洞（身份层 / 音频 / 发布）** → 两评测一致确认，未因泡测改变，仍是最高优先级。

---

## 2. 评测结论明细

### 2.1 六维一览表（含泡测后上调项）

| 维度 | 原评估 | 泡测后修正 | 一句话 |
|---|---|---|---|
| 设计 | 反馈 PASS／循环 CONCERNS／身份 CONCERNS／叙事 FAIL | 不变 | 理念层强、结构层空 |
| 工程 | 双库漂移 FAIL／其余 CONCERNS | 不变（泡测验证"可运行"≠"架构健康"） | Godot 必须归档 |
| QA | 纪律 PASS／体感 CONCERNS／**可玩放行 FAIL** | **可玩放行 → PASS（泡测实证全弧贯通、无软锁）**；体感仍 CONCERNS | 跑得通 ≠ 好玩 |
| 美术 | 基调 PASS＋4 CONCERNS | 不变 | 画卷成立，细节待磨 |
| 发布 | 上架 CONCERNS／版本·云·本地化·灰度·运营全 FAIL | 不变 | 离上线最远 |
| 音频 | FAIL | 不变（泡测亦确认无任何 WebAudio 钩子） | 画卷断在声音 |

### 2.2 相比原评估的代码级修正（主理人实证）
1. **开局序列**：实测为 1 卷首（4 幕文字幻灯，无音效无交互）+ `race_choice` 弹窗 + 两文字弹窗「封神修道录」「榜文远眺」。**共 4 个弹窗**，6.0 §一"前 60 秒零弹窗"未履行，但比"全程 inline 弹窗"略好。
2. **榜文显示**：渲染的是 `pressure.label`+`pressure.tip` 文字谜面，0–100 的 `value` 不渲染成条——符合 6.0，残留问题是 progression 仍线性。
3. **封神图卷被低估**：`world-scroll.js` 山河一瞥用 `fogged` 雾化未至之地 + 锁卡 + 叙事副标题，渐显/留白执行其实很强，应**上调评价**。
4. **音频缺失被放大**：`playPrologue` 全程无任何 WebAudio 钩子，6.0 列的开局"水滴→雷→按住首息"引子在代码里是**静默文字幻灯**。

### 2.3 Playwright 关键发现（两个"惊悚症状"实为测试台缺陷，非游戏缺陷）

| # | 症状 | 根因（harness bug） | 证据 / 修复 |
|---|---|---|---|
| H1 | 弹窗无限重复、推进停滞 | 游戏采用「UI 点击关闭弹窗」模型，`chooseRace/confirmBreakthrough/finishBattle` 等方法**不**自动 `popupQueue.shift()`；初版机器人处理完不移除弹窗 | 处理后显式 `shift()` → 推进恢复 |
| H2 | 2995 连败、卡 rq_10 | `BattleEngine.create()` 中 `manual: !!state.flags.battle_manual`，新档默认 `battle_manual=true`；手工模式下 `autoStep` 直接返回 `{acted:false}`，一张牌不出 → 必败 | 进战前置 `battle.manual=false` → 2 战 2 胜、全弧直达 |

> ⚠️ **给设计/制作的真实提醒（非缺陷，但建议改）**：游戏默认新玩家进入**手工卡牌战斗**，自动战斗需手动切换。这是合理设计（卡牌策略是核心玩法），但建议在 **onboarding / 首次破劫 UI** 更显眼地提示 自动/手动 切换，避免新手误以为战斗"打不动"。

### 2.4 风险清单（复用 `评估与门控建议.md` §4，标注泡测后变化）

| 级 | 风险 | 变化 |
|---|---|---|
| 🔴 P0 | 无真人 Playtest，发布体验未知 | **泡测仅覆盖"可运行"，未覆盖"好玩"**——P0 依旧，更尖锐 |
| 🔴 P0 | 双代码库漂移（Godot + 根 `data/` 仅服务废弃 Godot） | 不变，且 `CLAUDE.md` 仍含 Godot 命令（见 §3 M4） |
| 🔴 P0 | 发布/运营零基础（无云存档·版本·本地化·版号） | 不变；`web/js/save-manager.js` 实测仅 localStorage |
| 🟠 P1 | 身份层仅数字被动，缺"我是谁" | 不变；势力学派门控逻辑同构、无阻塞迹象 |
| 🟠 P1 | 零音频，画卷哲学无法闭环 | **实测 `web/js` 全目录无任何 AudioManager/audio 钩子**，确认 |
| 🟠 P1 | 开局 4 弹窗、榜文被讲解（违 6.0） | 不变 |
| 🟡 P2 | 移动端字号偏小、无 reduced-motion | 不变 |
| 🟡 P2 | 审计未接 CI、种族效果硬编码、Game 单对象过重、热路径全量存档 | 不变 |
| 🟡 P2 | 19 素材不全接（山河图 5 分层、9 CSS、5 VFX） | 不变 |

---

## 3. 评测后「文档修改项」清单（核心交付）

> 下列每一项均为**建议**。标注【修改】= 在现有文档上增删改；【新增】= 需要新建文档/文件。所有路径均已在本次评测中核对存在。

| # | 目标文档 / 路径 | 当前问题（实测） | 建议修改 | 优先级 | 类型 |
|---|---|---|---|---|---|
| **M1** | `评估与门控建议.md` | §1 QA 维度"可玩放行 FAIL"与 Playwright PASS 矛盾 | 将 QA「可玩放行」上调为 **PASS（可玩性）／体感 CONCERNS**；新增 **§7 实操验证附录** 引用 `playtest-report.md/.json`，并标注两轮评测的"可运行 vs 好玩"边界 | P0 | 【修改】 |
| **M2** | `design/6.0 从数值游戏到画卷…` | ① §一"前60秒零弹窗/择跟脚推迟"与实测 4 弹窗不符；② 封神图卷评价偏低；③ 音频钩子列为开局第一息但代码静默；④ 身份第五层仍是空层 | ① 加"开局实证"标注（4 弹窗定调失败）；② 上调封神图卷评价；③ 标注"音频未落地，画卷哲学未闭环"；④ 身份层标注"待 `design/7.x` 立项" | P1 | 【修改】 |
| **M3** | `美术需求.md` | **零音频条目**（grep 确认无 音频/音效/音乐/audio/BGM 任何匹配） | 新增「音频需求」章节：AudioManager（Web Audio）规格、环境音床（吐纳/破劫）、关键 SFX（卡牌/破劫/机缘）、与美术/音频门验收标准；并补可访问性条目（移动端字号、reduced-motion 前庭风险，对应 P2） | P1 / P2 | 【修改·新增章节】 |
| **M4** | `CLAUDE.md` | ① Commands 段仍含 `open -a Godot` / `godot --check-only`，Godot 已归档；② §7"网游/长期运营"与现状（localStorage 单机）背离，易误读为已具备 | ① 标注 Godot 命令废弃或移除；② §7 显式标注"当前为 MVP 单机过渡，云存档/服务端为 P0 发布门待补" | P2 | 【修改】 |
| **M5** | `devlog.md` | 按 CLAUDE §核心规范2，多轮评测（多 Agent + Playwright）尚未留痕 | 追加「评测里程碑」条目：两轮评测结论、本汇编文档产出、待办文档修订项 | P2（纪律） | 【修改·追加】 |
| **M6** | `design/7.x`（新建） | 身份层 4 势力机制、发布规划均无验收标准文档 | 新增 `design/7.0 身份层机制验收`、`design/7.1 势力/种族独有机制`、`design/7.2 发布规划（云存档·版本·本地化·版号）`，均须"验收标准先行（≥95）" | P1 | 【新增】 |
| **M7** | 工程规范 / `.github/workflows` | 审计（`audit_completeness.js`/`audit_integrity.js`）未接 CI；种族效果散落硬编码、`Game` 单对象过重、热路径全量存档 | 补 CI 工作流将双审计设为 PR 门禁；在工程规范补"种族效果数据化（`race_effects` 表）、拆分 `Game`、热路径存档降频"条目 | P2 | 【新增 CI】/【修改 规范】 |
| **M8** | `test/playtest/`（新建，纳入项目） | 泡测脚本在隔离工作区 `~/.workbuddy/binaries/node/workspace/playtest.mjs`，未纳入项目、无回归保障 | 将泡测脚本 + `playtest-report.*` 纳入 `test/playtest/`，并接 `.github` 回归门禁（全弧遍历 PASS 方可合并） | P2（回归） | 【新增】 |

**汇总**：【修改】5 项（M1 M2 M3 M4 M5），【新增】3 项（M6 M7-CI M8），另有 M7 规范部分属【修改】。优先级分布：P0×1、P1×3、P2×4。

---

## 4. 不修改说明

- 本轮严格遵守"只做评测不做修改"：未对 `评估与门控建议.md`、`playtest-report.md/.json`、`design/6.0`、`美术需求.md`、`CLAUDE.md`、`devlog.md`、`web/` 代码等任何文件执行写入或编辑。
- 所有"文档修改项"（§3）均为**建议清单**，待你拍板后由对应工作流（先写验收标准 ≥95）落地。
- 评测证据留存：`playtest-report.json`（结构化）、`playtest-report.md`（QA 报告）、`/tmp/playtest_shots/realm_*.png`（每跨境界截图）仍在原处。

---

## 5. 建议的下一步

1. **确认 §3 文档修订清单**：建议先落 P0（M1 自评测自洽）+ P1（M2/M3/M6 身份层与音频需求），再 P2。
2. **补一次真人 Playtest**：Playwright 只证"可运行"，"好不好玩"仍空缺（P0 风险首项）。建议按 `design/2.2` 黄金开局 + `design/1.8` 前 30 分钟链路走查，用 6.0「像画卷吗」打分。
3. **是否开第三轮评测**：若你希望测试台覆盖势力/本命（当前 `didFaction=false`/`didBenming=false` 因止于 `dx_01` 未触发），可将泡测终点延后到 `dx` 后续境界（增大 `MAX_ITER`）。

---

*附：本文档为两轮评测（多 Agent 六维评估 + Playwright 全弧 QA）的合并汇编，不替代 `评估与门控建议.md` 与 `playtest-report.md` 任一产物，而是为其建立索引与修订索引。*
