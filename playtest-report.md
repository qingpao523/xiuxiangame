# 封神修道录 · Playwright 加速全弧泡测报告

> 执行方式：Playwright（Chromium headless）驱动 Web 版本，`?debug=1` 入口 + `Game.fastForward()` 加速，目标为「全弧遍历」——从开局一路推进到地仙（`dx_01`）。
> 执行时间：2026-08-18 ｜ 引擎：Godot 已归档，Web 为主实现。

---

## 1. 结论速览

| 维度 | 结果 | 说明 |
|------|------|------|
| 启动稳定性 | ✅ PASS | 全程 0 console error / 0 page error / 0 失败请求 |
| 全弧可通关性 | ✅ PASS | 21 个境界（炼气士→真人→地仙）全部可达，到达 `dx_01` |
| 战斗系统 | ✅ PASS（修复后） | 2 次破劫 2 胜；战斗数值可赢，无崩溃 |
| 系统贯通 | ✅ PASS | 种族 / 本命法宝 / 道友(6) / 机缘(4) / 目标 / 破劫 均被触发 |
| 软锁 / 死循环 | ✅ 无 | 无卡死、无未捕获异常 |

**工程侧 QA 判定：PASS。** 游戏可稳定启动、可完整通关、无报错、无软锁。

---

## 2. 测试方法论

- 本地起 `python3 -m http.server 8137`（以 `web/` 为根，因 `data-manager.js` 用 `fetch("data/...")` 读表）。
- Playwright 加载 `http://localhost:8137/?debug=1`，`waitForFunction` 等待 `Game.state` 就绪。
- 驱动循环每步消化弹窗队列（处理完即 `popupQueue.shift()` 模拟 UI 点击关闭），再按 `getMainAction().type` 推进：`race/faction/benming/treasure/event/breakthrough_confirm` 处理弹窗，`level_up/claim/action/idle` 走主按钮；动作以「时间 hack」瞬间完成，每 5 步 `debugAddResources()+fastForward(600)` 模拟长线推进。
- 战斗：将战斗切到**自动模式**后，循环 `BattleEngine.autoStep` 直到 `b.done`，再 `finishBattle`。
- 卡死检测：连续 40 步状态无变化则清队列+补资源恢复。抵达 `dx_` 即视为全弧达成并提前结束。

---

## 3. 关键发现

### 3.1 早期「2995 连败」是测试台缺陷，非游戏缺陷（已修复）
初版泡测机器人吃下了 **2995 场破劫、0 胜**，且始终卡在 `rq_10`。经逐层排查定位为两处 harness bug：

1. **弹窗未被关闭**：游戏采用「UI 点击关闭弹窗」模型，`Game.chooseRace/confirmBreakthrough/finishBattle` 等方法**不会**自动 `popupQueue.shift()`。初版机器人处理完弹窗却不移除它，导致同一弹窗被无限重复处理，推进停滞。
2. **破劫战斗默认手工模式**：`BattleEngine.create()` 中 `manual: !!state.flags.battle_manual`，而新档默认 `battle_manual = true`。在手工模式下 `autoStep` 直接返回 `{acted:false}`，一张牌都不出 → 必败。机器人误以为「自动战斗」在跑，实际从未出牌。

修复：处理弹窗后显式 `shift()`；进入战斗前置 `battle.manual = false`（即切到游戏内已有的「自动战斗」开关）。**修复后再跑：2 场破劫 2 胜，全弧直达地仙。** 证明战斗数值本身健全。

> ⚠️ **给设计/制作的提醒**：游戏默认新玩家进入**手工卡牌战斗**（自动战斗需手动切换）。这本身是合理设计（卡牌策略是核心玩法），但建议在 onboarding / 首次破劫处更显眼地提示自动/手动切换，避免新手误以为战斗「打不动」。

### 3.2 完整推进链路验证
- 开局序章 → 种族选择 → 封神修道录/榜文远眺 → 练气十重（`rq_01→rq_10`）→ 首破劫（胜）→ 真人十重（`zr_01→zr_10`）→ 二破劫（胜）→ 地仙（`dx_01`）。
- 期间触发：本命法宝择主、机缘事件 4 次、道友招募 6 位、目标系统推进、离线收益领取。

### 3.3 未覆盖的深层门控（非缺陷，仅本次遍历未触及）
- **势力选择（`didFaction=false`）**：仅在 `RealmManager.isCapped() && !faction_id` 时触发，需抵达某大境尽头。本遍历在 `dx_01`（地仙刚入门，仍有后续境界）即结束，故未触发。
- **本命流派（`didBenming=false`）**：仅在 `bt_003`（真仙破劫）后触发，位于地仙更深处，本次未达。
- 这两项是「到达即解锁」的晚期门控，逻辑与前面门控同构，未发现阻塞迹象；如需 100% 门控覆盖，可继续向 `dx` 后续境界推进。

---

## 4. 稳定性数据（全程）

- 控制台错误：**0**
- 页面异常（pageerror）：**0**
- 失败请求（requestfailed）：**0**
- 驱动层错误（drive error）：**0**
- 软锁恢复触发：**0 次**（全程未进入卡死恢复分支）

机器人在 dx_01 收尾时的状态：`realm=dx_01, race=human, companions=6, treasures=1, events=4, rebirth=0, god_seats=0`，无残留异常弹窗。

---

## 5. 测试局限（诚实声明）

1. **机器人是机械驱动，不评估「好不好玩」**：只能验证「能不能跑通、有没有报错、数值能不能赢」，无法判断手感、节奏、叙事沉浸等体验维度——这些仍须人工试玩（你作为主理人的核心验收标准）。
2. **加速破坏了数值平衡校验**：每 5 步注入资源 + `fastForward` 让破劫道行门槛被轻易满足，因此**本测试不验证经济/数值平衡**，只验证「在足够资源下弧线能否贯通」。平衡需单独用真实节奏的长线存档评估。
3. **手工战斗未经胜率验证**：本次用自动模式跑通战斗。真实玩家走手工模式，其卡牌出牌策略、胜率、是否过难，需真人实战或专门的对战 AI 验证。
4. 截图证据见 `/tmp/playtest_shots/realm_*.png`（每跨境界一张）。

---

## 6. 建议的下一步

- [ ] 真人对「手工战斗」首破劫难度做主观验收（对照设计哲学「前30分钟最高优先级」）。
- [ ] 若希望测试台覆盖势力/本命，可将遍历终点延后到 `dx` 后续境界（增大 `MAX_ITER` 即可）。
- [ ] 考虑在首次破劫 UI 强化「自动/手动」切换提示。

---

*附：结构化数据见同目录 `playtest-report.json`。测试脚本位于隔离工作区 `~/.workbuddy/binaries/node/workspace/playtest.mjs`。*
