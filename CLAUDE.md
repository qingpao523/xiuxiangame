# CLAUDE.md

This file provides guidance to Claude Code when working with this project.

## 基本原则

**基于第一性原理解决一切问题。**
遇到任何问题，回到最底层原理做推理，不依赖经验、惯例或猜测。拆解到不可再分的物理/数学/逻辑事实后重建解决方案。

所有开发工作严格遵循这一原则——不接受"大家都这么做"、不依赖黑盒猜测、每一步都要有可追溯的根因推理。

## 产品守则（不可违背，2026-08-16 确立）

1. **好玩优先于功能堆叠**：玩家进入的是封神大劫将起的世界，不是数据面板。任何新系统先回答“它给玩家带来了什么选择、风险或世界信息”，回答不了就不做。
2. **不是点点点，而是参与封神**：每次主循环操作都要有世界观反馈。放置只是时间尺度，不是把玩家变成结算按钮的点击器；战斗、破劫、机缘、势力选择是“参与”，必须保留可操作性和后果。
3. **前 30 分钟体验是最高优先级**：遵循 `design/2.2` 黄金开局 checklist——首反馈 ≤30 秒、首战斗 ≤3 分钟、首次机缘 8-12 分钟、首次大突破 15-25 分钟、无 >60 秒干等、一次只教一件事。
4. **弹窗克制**：一次玩家行动最多只允许一个关键模态弹窗；重复性奖励用非阻断浮字/日志。奖励必须与行为分量匹配，禁止“每轮都发心得/三选一”式奖励通胀。
5. **不强制养成路线**：术法、法宝、Boss、游历、破劫是并列选择，主按钮只推荐，不卡死；任何目标都不许成为唯一可玩路径。
6. **世界观是玩法的一部分**：所有新增系统必须挂接封神锚点（榜文/劫气/功德/阐截/杀劫/神位），没有封神包装的系统不得进入版本。
7. **这是手游、网游、长期文字修仙**：设计任何系统都要默认“账号在服务端、玩家在移动端、内容按赛季/版本推进”，并预留社交、排行榜、稀缺位、云存档接口。单机存档只是当前 MVP 的过渡形态。
8. **先读文档再动手**：开发前必须读 `devlog.md` 最新记录，以及 `design/1.8`（前 30 分钟链路）、`design/2.0`（中长期路线）、`design/2.2`（黄金开局方法论）。违反前 30 分钟守则的改动不得提交。

## 项目简介

封神·放置修仙（竖屏文字修仙放置游戏）。纯网页版：HTML/CSS/JS，位于 web/，浏览器打开 web/index.html 即玩。已移除 Godot 引擎，统一为单一 Web 技术栈，数据唯一来源为 web/data/。

## 核心规范

1. **第一性原理驱动**：任何实现方案、Bug 修复、性能优化，必须先回到底层原理推导，再动手。不准凭经验猜。
2. **每次更新代码后必须在 `devlog.md` 追加记录**：日期、变更摘要、关键决策理由。不写日志不准提交。
3. **每次开发前先看 `devlog.md`**：了解项目演进脉络、已解决的问题、尚未完成的工作，避免重复劳动或走回头路。
4. **代码审查**采用对抗性审查方式审查代码。
5. **美术素材流程**：开发中需要美术元素时，必须在 `美术需求.md` 中追加对应条目，包含：① GPT-image-2 生图提示词（英文，含风格/尺寸/构图/色调）；② 验收标准（分辨率、透明通道、适配场景、与现有风格一致性）；③ 标注"初版/待替换"状态。开发时先用 CSS/SVG/占位图跑通逻辑与交互，美术素材到位后按层替换，不改逻辑代码。禁止因等待美术而阻塞功能开发。
6. **验收标准先行（不可违背）**：任何任务（新系统、改造、内容批次）动手前，必须先写出该任务的**验收标准**（评分维度 + 权重 + 打分制，满分 100）。然后才实现。实现完成后，逐项对照标准打分：**≥95 分方可通过验收**；<95 分一律打回重做，直至达标。验收标准与得分须记入 `devlog.md`。禁止"先做再说、做完不验收"。

## Commands

```bash
# 运行网页版（浏览器打开 http://localhost:8090）
cd web && python3 -m http.server 8090

# 数据审计（跨系统引用一致性 + 实现完整性）
node audit_integrity.js
node audit_completeness.js

# JS 语法检查
for f in web/js/*.js; do node --check "$f"; done
```
