# 封神·放置修仙 — 项目评估与发布门控建议

> 汇编：游戏开发工作室主理人 · 游承峰
> 日期：2026-08-18
> 范围：GAME-XIUXIAN 手游项目（Godot / Web / 微信小程序三栈）
> 评审强度：full 完整门控
> 上架目标（已确认）：**Web 版**（web/ 21 个 JS + data 19 张 JSON），经微信小程序 webview 壳上架；Godot 工程视为废弃旧构建

---

## 0. 项目画像与阶段判定

**《封神·放置修仙 / 封神修道录》**——以"封神大劫"为背景的文字放置修仙手游。

- **Web 主实现**：`web/js/` 21 个 JS + `web/data/` 19 张 JSON（数据驱动），活跃迭代版本
- **Godot MVP**：`scenes/` + `scripts/autoload/` 9 个 .gd，早期 MVP，**已停滞**（缺失全部 6.x 系统）
- **微信小程序**：`wechat/miniprogram-webview/` 为 webview 壳

**工程纪律极强**：第一性原理驱动、验收标准先行（≥95 分通过）、对抗性代码审查、两个审计工具（`audit_completeness.js` / `audit_integrity.js`）。

**阶段判定**：制作（Phase 5）尾段 → 打磨（Phase 6）初段之间。核心循环已跑通、内容已做厚，最近提交为审计/审查（Phase 6 性质）。但 `design/6.0` 六大改造层里，**身份层（势力/种族机制）整层未动**。

---

## 1. 多 Agent 六维评估（各成员独立只读评估，零文件写入）

| 维度 | 评审 | 判定 | 一句话 |
|---|---|---|---|
| 设计 | 文策渊 | 反馈层 **PASS**／核心循环 **CONCERNS**／身份层 **CONCERNS**／叙事非线性 **FAIL** | 理念层强、结构层空 |
| 工程 | 程基岩 | 双库漂移 **FAIL**／Web架构·审计·扩展·性能均 **CONCERNS** | Godot 必须归档 |
| QA | 严守真 | 纪律 **PASS**／体感 **CONCERNS**／可玩放行 **FAIL** | 做完没玩过 |
| 美术 | 林绘澄 | 基调 **PASS**＋4 条 **CONCERNS** | 画卷成立，细节待磨 |
| 发布 | 路远行 | 上架路径 **CONCERNS**／版本·云存档·本地化·灰度·运营门控全 **FAIL** | 离上线最远 |
| 音频 | 阮和鸣 | **FAIL**（零音频） | 画卷断在声音 |

### 各维度要点

- **设计**：三层反馈（微文案/小仪式/大画卷）强落地；但开局仍是 4 幕文字幻灯、身份层仅数字被动、叙事非线性未真正成立。
- **工程**：Godot 仅为 Web 约 10–15% 的早期骨架，`data/` 与 `web/data/` 重复；Web 用全局命名空间靠 `<script>` 加载序隐式耦合；`Game` 单对象 1300 行过重；热路径全量 `JSON.stringify` 写 localStorage；审计未接入 CI、种族效果散落硬编码。
- **QA**：验收纪律扎实，但 100 分验证的是"实现存在且设计上对"，**feel/好玩维度被默认满分**；审计盲区覆盖不到 UI 体感、数值平衡、长线旧档、移动触控；无 Bug 分级体系。
- **美术**：画卷基调成立（留白/渐显/气韵真实落地）；19 素材不全接（山河图 5 分层必联调、9 个 CSS 皮肤续用更精、5 个 VFX 因破劫改画卷演出成孤儿）；移动端字号偏小、无 reduced-motion（前庭风险）。
- **发布**：Web+微信路径基本通但 gameUrl 为空、缺备案域名；无语义版本/构建元数据；纯 localStorage 无云存档/服务端账号，与 CLAUDE.md #7「网游/长期运营」背离；无 i18n；无 staging/回滚/热修。
- **音频**：代码/素材/目录三端皆空，`美术需求.md` 连一条音频条目都没有，需求断层。

---

## 2. 跨维度共识（三个被反复点名的真问题）

1. **"做完没玩过"——验收分全是水分**（QA+设计+工程一致）。100 分 = 实现且设计上对 ≠ 好玩；审计不覆盖 UI 体感/数值平衡/长线旧档/移动触控。
2. **双代码库漂移**（工程 FAIL）。Godot 仅 9 .gd 早期骨架，根目录 `data/` 与 `web/data/` 重复且只服务废弃 Godot。**必须归档 Godot + 删 `data/`**。
3. **身份层 + 音频 + 发布，是 6.0/CLAUDE.md 愿景里完全塌掉的三块**。身份层仅数字被动；音频三端皆空；发布侧云存档/服务端/版本号/本地化/版号全缺。

---

## 3. 代码级深度验证与两处修正（主理人实证核对）

> 多 Agent 评估靠"读代码猜体验"，有两处偏差，经行级实证修正。

### 3.1 开局序列实证（game.js:66-93 + world-scroll.js:5-156）
新玩家启动真实顺序：
1. `PROLOGUE_SCENES` 4 幕文字卷首（自动/点击推进，**无音效、无交互**，`world-scroll.js:5-31`）
2. 紧接 `race_choice` 择跟脚弹窗（`game.js:76`）
3. 再叠两文字弹窗「封神修道录」「榜文远眺」（`game.js:77-82`）

**结论**：6.0 §一要求的"前 60 秒零弹窗""择跟脚推迟到首次游历"**均未做到**；榜文被「榜文远眺」弹窗直接讲解。设计维度"开局仍是 4 弹窗定调失败"——属实（1 卷首 + 3 弹窗），但比 6.0 原批评略好（非全程 inline 弹窗）。

### 3.2 修正一：榜文显示并非直读进度条（原评估误判）
`world-scroll.js:221,229` 显示的是 `pressure.label`（"榜文微照"）+ `pressure.tip`（叙事文案）；0–100 的 `value` 仅内部阈值用，**不渲染成条**。可见 UI 已是文字谜面，符合 6.0；残留问题是 progression 仍线性（渐近→微照→牵引→受牵）。

### 3.3 修正二：封神图卷被低估（原评估过严）
`world-scroll.js:233-243` 山河一瞥用 `fogged` 雾化未至之地、`"此卷尚未揭开"` 锁卡 + 叙事副标题，**渐显/留白执行其实很强**，应上调评价。

### 3.4 音频缺失被放大
`playPrologue`（`world-scroll.js:95-156`）全程无任何 WebAudio 钩子。6.0 把"水滴声→雷声→按住首息"列为开局第一息引子，但代码里 prologue 是**静默文字幻灯**——把音频维度 FAIL 升级为"开局沉浸直接断在声音上"。

---

## 4. 优先级风险清单

| 级 | 风险 | 来源 |
|---|---|---|
| 🔴 P0 | 无真人 Playtest，发布体验未知 | 严守真 |
| 🔴 P0 | 双代码库漂移，维护即踩坑 | 程基岩 |
| 🔴 P0 | 发布/运营零基础（无云存档·版本·本地化·版号） | 路远行 |
| 🟠 P1 | 身份层仅数字被动，长线留存缺"我是谁" | 文策渊/程基岩 |
| 🟠 P1 | 零音频，画卷哲学无法闭环 | 阮和鸣 |
| 🟠 P1 | 开局仍是 4 弹窗、榜文被讲解（违 6.0） | 文策渊/林绘澄 |
| 🟡 P2 | 移动端字号偏小、无 reduced-motion（前庭风险） | 林绘澄 |
| 🟡 P2 | 审计未接 CI、种族效果散落硬编码、Game 单对象过重、热路径全量存档 | 程基岩 |
| 🟡 P2 | 19 素材不全接（山河图 5 分层必联调，9 CSS 续用，5 VFX 不接） | 林绘澄 |

---

## 5. full 门控下的建议路线（1–5）

按"先证伪体验、再补空洞、最后发布"顺序，每门均先写验收标准（≥95 通过）再实现：

1. **体验评审门** — 先做首次整体 Playtest（前 30 分/前 7 天/破劫/长线/旧档/移动触控走查），用 6.0「像画卷吗」标准打分，把"好不好玩"从假设变证据。严守真已给走查清单草案。
2. **架构门** — 归档 Godot + 删 `data/`、审计接 CI 发布门禁、种族效果数据化（`race_effects` 表）、拆分 `Game` 单对象、热路径存档降频。
3. **设计门** — 重做开局环境体验（黑屏→水滴→自见榜文→按住首息，复用 ScrollScene，加音频钩子）、榜文去讲解化、身份层 4 势力机制（design/7.x 验收先行）。
4. **美术/音频门** — 山河图 5 分层坐标联调、移动端字号+可访问性补完；补 `美术需求.md` 音频条目并立项 AudioManager（Web Audio），先于破劫/吐纳节点接入环境音床+关键 SFX。
5. **发布门** — 接入服务端账号+云存档（先 CloudBase）、CI 改投微信可备案 HTTPS 域名并填实 gameUrl、建立语义版本+构建元数据+tag、引入 i18n 字符串表、补齐版号/ICP/隐私/反作弊/埋点及 staging+回滚预案。

---

## 6. 关键决策记录

- **上架目标 = Web 版**（用户确认 2026-08-18）。Godot 工程不再维护，建议归档。
- **评审强度 = full 完整门控**（每阶段过设计/架构/烟雾/发布评审，≥95 验收）。
- **身份层（势力/种族独有机制）** 是 6.0 六层里唯一几乎空着的层，钩子大多已就绪（`chooseFaction`/`chooseRace`、`action_table` 的 `faction`/`race` 条件、`treasure_shard` 货币、战斗开局状态注入），主要阻塞为截教"阵法卡"依赖未建的"布阵"系统。
- **本文件为评估汇编，非代码改动**；后续任一工作流启动前须先写对应验收标准。
